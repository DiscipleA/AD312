import { useMemo, useState } from 'react'
import CodeBlock from '../components/CodeBlock'
import ErrorBoundarySafetyNetTestPanel from './ErrorBoundarySafetyNetTestPanel'
import solutionSyntax from '../assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.jsx?raw'
import helperSyntax from '../assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.helpers.js?raw'
import consoleTestSyntax from '../assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.console-tests.js?raw'
import vitestSyntax from '../assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.test.jsx?raw'
import { SafetyNetDashboard, marketScenarios, summarizeMarketScenario } from '../assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.jsx'
import '../styles/week10-error-boundary-assignment.css'

const starterSyntax = `class MarketWidgetErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error) {
    return { hasError: true };
  }

  render() {
    if (this.state.hasError) {
      return <p>Market widget is temporarily unavailable.</p>;
    }
    return this.props.children;
  }
}`
function makeEventId() { return typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function' ? crypto.randomUUID() : `event-${Date.now()}-${Math.random().toString(16).slice(2)}` }

export default function Week10ErrorBoundaryAssignmentGuide() {
  const [selectedScenarioId, setSelectedScenarioId] = useState('healthy')
  const [boundaryEvents, setBoundaryEvents] = useState([])
  const scenario = marketScenarios[selectedScenarioId]
  const summary = useMemo(() => summarizeMarketScenario(scenario), [scenario])
  function selectScenario(nextScenarioId) { setSelectedScenarioId(nextScenarioId); setBoundaryEvents([]) }
  function recordBoundaryError(errorInfo) { setBoundaryEvents((events) => [{ id: makeEventId(), message: errorInfo.message }, ...events]) }
  return <article className="error-boundary-assignment-guide"><header className="error-boundary-hero-card error-boundary-card"><p className="error-boundary-kicker">AD312 • Week 10 • Assignment 02</p><h1>Building an Application Safety Net with Error Boundaries in React</h1><p>Build a social media dashboard with a live market price widget, intentionally break that widget with corrupted data, catch the crash with a class-based Error Boundary, and keep the rest of the application online.</p></header><section className="error-boundary-card"><h2>Assignment Objective</h2><p>The completed work shows why Error Boundaries matter in production React apps: one unsafe widget should not blank the entire dashboard. The lab uses a class component with <code>getDerivedStateFromError</code>, displays a friendly fallback UI, keeps surrounding content functional, and validates the behavior through GUI checks, console tests, and Vitest tests.</p></section><section className="error-boundary-card"><h2>Required Test Coverage</h2><ul className="error-boundary-checklist"><li>Healthy market widget renders normally.</li><li>Volatile but valid market data still renders and charts correctly.</li><li>Market chart data produces one SVG point per history value.</li><li>Corrupted market payload triggers the fallback UI.</li><li>Missing market payload triggers the fallback UI.</li><li>Explicit render-time widget crash is isolated from the rest of the dashboard.</li></ul></section><section className="error-boundary-preview-card"><div className="error-boundary-preview-header"><p className="error-boundary-kicker">Working Preview</p><h2>Social Trading Dashboard Safety Lab</h2><p>Switch between healthy market data, corrupted server responses, missing payloads, and a forced third-party widget crash. The charted market widget may fail, but the social dashboard remains usable.</p></div><div className="error-boundary-actions" aria-label="Market scenario controls">{Object.values(marketScenarios).map((item) => <button key={item.id} type="button" className={item.id === selectedScenarioId ? 'error-boundary-active-action' : 'error-boundary-secondary-action'} onClick={() => selectScenario(item.id)}>{item.label}</button>)}</div><div className="error-boundary-result-grid"><article className={summary.expectedBoundary ? 'error-boundary-warn' : 'error-boundary-pass'}><span>Selected Scenario</span><strong>{scenario.label}</strong><small>{scenario.description}</small></article><article><span>Expected Behavior</span><strong>{summary.expectedBoundary ? 'Fallback UI should appear' : 'Market widget should render'}</strong><small>{summary.reason}</small></article><article className="error-boundary-pass"><span>Dashboard Isolation</span><strong>Social feed remains available</strong><small>The Error Boundary wraps only the risky market widget.</small></article><article><span>Boundary Log</span><strong>{boundaryEvents.length ? `${boundaryEvents.length} crash event recorded` : 'No crash recorded yet'}</strong><small>{boundaryEvents[0]?.message ?? 'Choose a corrupted or crash scenario to see the boundary log update.'}</small></article></div><SafetyNetDashboard key={selectedScenarioId} scenario={scenario} onBoundaryError={recordBoundaryError} boundaryKey={selectedScenarioId} /></section><ErrorBoundarySafetyNetTestPanel /><section className="error-boundary-card"><h2>Starter Shape</h2><p>This compact shape shows the minimum class-component boundary. The completed source below expands it with detailed comments, reset behavior, fallback UI, dashboard isolation, charts, validation helpers, and tests.</p><CodeBlock language="jsx" label="Starter Error Boundary Shape" code={starterSyntax} /></section><section className="error-boundary-card error-boundary-analysis-card"><h2>Why Class Components Are Still Used</h2><p>React Error Boundaries still rely on class lifecycle APIs. A boundary must implement <code>static getDerivedStateFromError</code> to convert a render crash into safe fallback state, and it may use <code>componentDidCatch</code> to log the error. Function components and hooks are excellent for most UI state, but React does not currently expose an equivalent built-in hook that catches render errors from child components in the same official boundary pattern.</p></section><section className="error-boundary-card"><h2>Full Source Code</h2><p>The completed source includes the class-based Error Boundary, the risky market widget, SVG chart helpers, scenario validation helpers, and the social dashboard preview.</p><CodeBlock language="jsx" label="src/assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.jsx" code={solutionSyntax} /></section><section className="error-boundary-card"><h2>Helper Source Code</h2><p>The helper file keeps data validation and chart-coordinate logic testable from both the browser and the Node console test file.</p><CodeBlock language="js" label="src/assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.helpers.js" code={helperSyntax} /></section><section className="error-boundary-card"><h2>Console-Log Tests</h2><p>These console checks keep the non-DOM helper behavior easy to inspect from Node. They verify valid data, corrupted data, missing data, forced crash summaries, and chart-point generation.</p><CodeBlock language="js" label="src/assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.console-tests.js" code={consoleTestSyntax} /></section><section className="error-boundary-card"><h2>Vitest Tests</h2><p>The official Vitest file uses React Testing Library to verify normal rendering, fallback rendering, reset behavior, error logging, chart output, and dashboard isolation.</p><CodeBlock language="jsx" label="src/assignments/week10/error-boundary-safety-net/ErrorBoundarySafetyNet.test.jsx" code={vitestSyntax} /></section><section className="error-boundary-card"><h2>Manual Verification</h2><ul><li>Use the Working Preview buttons to load every market scenario.</li><li>Confirm healthy and volatile scenarios render the market widget and chart.</li><li>Confirm corrupted, missing, and forced-crash scenarios display the fallback UI.</li><li>Confirm the social feed remains visible after the market widget fails.</li><li>Confirm the Live Test Results panel passes all six required checks.</li><li>Run the raw helper console test file with Node.</li><li>Run the Vitest file for official automated checks.</li></ul></section><section className="error-boundary-card error-boundary-summary-card"><h2>Summary</h2><p>This assignment turns Error Boundaries into a practical application safety net. The dashboard demonstrates a realistic broken market widget, the fallback UI prevents a blank screen, the charted valid scenarios prove normal rendering still works, and the tests confirm both normal and edge cases.</p></section></article>
}
