# Syncing State with LocalStorage using a Custom Hook

Build a reusable `useLocalStorage` hook that combines React state with browser `localStorage` so user preferences survive page refreshes.

The runtime guide includes a persistent streaming preference preview, charts, a mock API synchronization panel, Live Test Results, full source code, and Vitest coverage.
